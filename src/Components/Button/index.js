import React from "react";
import StyledButton from "./styles"

const Button = () => {
    return (
        <StyledButton>
            <button></button>
        </StyledButton>
    )
}

export default Button;